#!/usr/bin/env python
PACKAGE = "velocity_estimator"

from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()

gen.add("matching_tolerance", double_t, 0, "Error tolerance[pixel] used in matching points between left, right, current, previous frames. Disabled when minus", 0.6, -1.0, 30.0)

exit(gen.generate(PACKAGE, "velocity_estimator", "VelocityEstimator"))
